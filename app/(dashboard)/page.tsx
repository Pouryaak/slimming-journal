import { SignOutButton } from '@/components/auth/SignOutButton';
import React from 'react';

const page = () => {
  return (
    <div>
      <SignOutButton />
    </div>
  );
};

export default page;
